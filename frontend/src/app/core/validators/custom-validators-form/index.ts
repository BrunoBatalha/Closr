import { minOneLetterLowerCaseValidator } from './minOneLetterLowerCaseValidator';
import { minOneLetterUpperCaseValidator } from './minOneLetterUpperCaseValidator';
import { needsNumberValidator } from './needsNumberValidator';

const CustomValidator = {
	minOneLetterUpperCaseValidator,
	minOneLetterLowerCaseValidator,
	needsNumberValidator
};

export { CustomValidator };
